<%- include('partials/header') %>

<main>

  <!-- Vehicle Header -->
  <div class="vehicle-header">
    <h1><%= vehicle.name %></h1>

    <p class="vehicle-category">
      Category: <%= vehicle.category %>
    </p>

    <p class="vehicle-meta">
      ID: <%= vehicle.id %> • $<%= vehicle.price %>
    </p>

    <p class="vehicle-description">
      <%= vehicle.description %>
    </p>
  </div>

  <!-- Specifications -->
  <div class="specs">
    <div class="sections-header">
      <h2>Specifications</h2>

      <div class="sort-options">
        <span>Sort by: </span>

        <a href="/vehicles/<%= vehicle.id %>?sort=feature"
           class="<%= currentSort === 'feature' ? 'active' : '' %>">
          Feature
        </a>

        <a href="/vehicles/<%= vehicle.id %>?sort=value"
           class="<%= currentSort === 'value' ? 'active' : '' %>">
          Value
        </a>
      </div>
    </div>

    <div class="specs-list">
      <% vehicle.specs.forEach(spec => { %>
        <div class="spec-card">
          <strong><%= spec.feature %>:</strong>
          <span><%= spec.value %></span>
        </div>
      <% }); %>
    </div>
  </div>

  <!-- Navigation -->
  <div class="navigation">
    <a href="/vehicles<%= vehicle.category ? '?category=' + vehicle.category : '' %>">
      &larr; Back to Vehicles
    </a>
  </div>

</main>

<%- include('partials/footer') %>
